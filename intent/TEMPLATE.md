<!-- 이 파일은 템플릿이다. 복사해서 intent/YYYY-MM-DD-슬러그.md 로 쓴다. 이 파일 자체를 고치지 않는다. -->

# Intent: (한 줄 제목 — 무엇을 만들/바꿀 것인가)
Author: (이름, 역할) · Status: draft

## Problem
지금 무엇이 불편한가. 누가 어떤 상황에서 막히는가.
해결책은 쓰지 않는다 — 문제만 쓴다.

## Proposed outcome
해결되면 무엇이 달라지는가. 사용자가 체감할 결과로 쓴다.
기술 선택·구현 방법은 쓰지 않는다(그건 플랜 모드의 몫).

## Affected users and systems
- 역할: 관리자 / 시험자 / 둘 다  ← 이 프로젝트는 권한 분기가 거의 모든 화면에 있다
- 화면: (경로)
- 테이블: (Supabase 테이블 이름)
- 확정(LOCK)·작업 시작 상태와의 관계: (LOCK된 오더면 어떻게 되나)
- 크론: (`instrumentation.ts` — PCT 적재 09:00/14:00 · 장비 대기 자동취소 매시 정각. 영향이 있나)

## Constraints
예산·기한·정책·금지사항. "없음"으로 비우지 않는다.
"기존 동작을 깨지 않는다"가 아니라 구체적으로 무엇을 깨지 않는지 쓴다.

## Open questions
아직 정하지 못한 것을 실제로 남긴다. **비어 있으면 안 된다.**
형식만 채운 질문은 쓰지 말고, 진짜 모르는 것을 쓴다.
