아래 분석 내용을 사용자 질문에 **자연스럽게 답하는 대화형 HTML**로 변환하세요.
Claude/ChatGPT가 답하듯 — 핵심을 먼저 말하고, 필요한 만큼만 구조를 사용합니다.
고정된 보고서 템플릿(표지·Executive Summary·섹션 번호)을 강제로 적용하지 마세요.

> 마크다운으로 답하는 시스템이면 [P0 출력 형식]·[스타일 규칙]만 마크다운 규칙으로 교체하고
> [구성 원칙]·[금지 사항]은 그대로 쓴다. 이 팩에서 진짜 값어치는 그 두 장이다.

━━━━━━━━━━━━━━━━━━━━━━━━━━
[P0 출력 형식 — 절대 위반 금지]
━━━━━━━━━━━━━━━━━━━━━━━━━━
- 응답의 첫 글자는 반드시 `<` 여야 한다. (예: `<div`)
- 절대 ```html, ```, <!DOCTYPE, <html>, <head>, <body> 로 시작/감싸지 말 것.
- 마크다운 코드펜스(```) 사용 금지. 설명 문구 없이 곧장 HTML 태그로 시작.
- 반드시 <div>로 시작해서 </div>로 끝낼 것.

━━━━━━━━━━━━━━━━━━━━━━━━━━
[스타일 규칙]
━━━━━━━━━━━━━━━━━━━━━━━━━━
1. inline style만 사용. class=, 유틸리티 클래스 절대 금지.
2. font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', 'Noto Sans KR', sans-serif
3. 기본 글자: font-size:14px; line-height:1.7; color:#374151
4. 링크(href)는 원본 그대로 유지. URL 절대 변경 금지.
5. 마크다운 강조(**, __)는 <strong>으로 변환. 별표 문자를 본문에 남기지 말 것.
6. 수치·날짜·고유명사·금액·인용은 원본 그대로 보존 (임의 생성·왜곡 금지).

━━━━━━━━━━━━━━━━━━━━━━━━━━
[구성 원칙 — 질문에 맞게 유연하게]
━━━━━━━━━━━━━━━━━━━━━━━━━━
▶ 첫 1~2문장: 질문에 대한 직접적인 답(결론)부터. 인사말·서론 금지.
▶ 그 다음, 내용 분량과 성격에 맞는 **최소한의 구조만** 사용:
   - 짧은 답(사실 확인·단일 정보) → 문단 1~2개로 끝. 표·헤더 만들지 말 것.
   - 항목 나열 → <ul> 불릿 (li 당 한 가지 팩트, margin:4px 0)
   - 수치·항목 비교 → 간단한 <table>
     (border-collapse:collapse; th/td는 padding:8px 12px;
      border-bottom:1px solid #e5e7eb; 숫자는 오른쪽 정렬)
   - 주제가 여러 개일 때만 소제목:
     <div style="font-size:15px;font-weight:700;color:#111827;margin:18px 0 8px">소제목</div>
▶ 강조가 필요한 핵심 결론·주의점은 **1개까지만** 카드 사용:
   <div style="background:#f0f6ff;border-left:3px solid #3b82f6;padding:10px 14px;border-radius:6px;margin:12px 0">…</div>
▶ 출처가 여러 개면 마지막에 한 줄로:
   <div style="font-size:12px;color:#6b7280;margin-top:14px">출처: 문서A · 문서B</div>
▶ **분량은 원본 내용에 비례** — 짧은 내용을 길게 늘리지 말고, 긴 내용은 핵심 위주로
   정리하되 질문과 관련된 팩트는 누락하지 말 것.

━━━━━━━━━━━━━━━━━━━━━━━━━━
[금지 사항]
━━━━━━━━━━━━━━━━━━━━━━━━━━
- 그라디언트 헤더·표지·EXECUTIVE SUMMARY·섹션 번호(1~9) 등 보고서 장치 금지.
- 질문과 무관한 메타 설명("이 문서는 ~로 구성되어 있습니다" 식 나열) 금지.
- 내용 없는 빈 섹션·"해당 없음" 채우기 금지 — 없는 항목은 아예 만들지 않는다.
- 사용자가 보고서 형식을 원하면 시스템이 별도 프롬프트를 사용하므로,
  여기서는 절대 보고서 형식으로 답하지 말 것.
- 내부 테이블명·컬럼명·시스템 식별자·처리 절차를 노출하지 말 것.

[참고] MODE = {mode}  (참고 정보일 뿐, 구성은 위 원칙대로 질문에 맞게 판단)

[사용자 질문 맥락]
{user_context}

[원본 분석 결과 — 위 원칙에 맞춰 자연스러운 HTML 답변으로 재구성하라]
{answer}
